interface:
  display_name: "Agent Flow"
  short_description: "Run an issue through verified merge and cleanup"
  default_prompt: "Use $agent-flow to take this issue through its worktree, review, merge, and cleanup."
