# Scenarios

Good: Inspect the clock plan and memory limits before asking whether a VGA frame
crossing should favor tearing-free output or lower memory use.

Bad: Ask facts available in the board manual, present ten unrelated questions,
or start implementation before the user confirms the decision packet.

Not a trigger: The user asks for ordinary repository work without saying
`grill-me` or `$grill-me`.
